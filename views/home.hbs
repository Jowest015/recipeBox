{{#if rows}}
    <table>
        <tr>
            <th>Name</th>
            <th>Ingredients</th>
            <th>Directions</th>
        </tr>
        {{#each res.rows}}
        <tr>
            <td>{{name}}</td>
            <td>{{this.ingredients}}</td>
            <td>{{this.directions}}</td>
        </tr>
        {{/each}}
    </table>
    {{else}}
    <h3>No recipes to show.</h3>
{{/if}}
